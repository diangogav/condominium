export enum UserRole {
    ADMIN = 'admin',
    BOARD = 'board',
    RESIDENT = 'resident'
}

export enum UserStatus {
    ACTIVE = 'active',
    PENDING = 'pending',
    INACTIVE = 'inactive',
    REJECTED = 'rejected'
}
